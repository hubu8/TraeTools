## TraeTools 运行与打包指南

### 环境要求

本项目基于 .NET 9 + Avalonia 12 构建，需要以下环境：

- **操作系统**：Windows 10 / 11（x64）
- **.NET SDK**：9.0 或更高版本（[下载地址](https://dotnet.microsoft.com/download/dotnet/9.0)）
- **WebView2 运行时**：Windows 10/11 通常已预装；如未安装可从 [Microsoft 官网](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) 下载

验证 SDK 是否安装成功：

```bash
dotnet --version
```

---

### 从源码运行

**1. 克隆或获取项目源码**

```bash
git clone <仓库地址>
cd TraeTools
```

**2. 还原 NuGet 依赖**

```bash
dotnet restore
```

这一步会下载 Avalonia、CommunityToolkit.Mvvm、WebView2 等依赖包，首次执行需要几分钟。

**3. 编译并运行（Debug 模式）**

```bash
dotnet run
```

或者分步执行：

```bash
dotnet build
dotnet run --no-build
```

程序启动后会显示主窗口，包含仪表盘、签到、切换、设置等页面。

---

### 发布为单文件可执行程序

发布后的 exe 自带 .NET 运行时，目标机器无需安装任何 SDK。

**1. 执行发布命令**

```bash
dotnet publish TraeTools.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o ./publish
```

各参数含义：

| 参数 | 说明 |
|------|------|
| `-c Release` | 发布模式，启用优化 |
| `-r win-x64` | 目标平台：Windows x64 |
| `--self-contained true` | 打包 .NET 运行时，目标机器无需安装 SDK |
| `PublishSingleFile=true` | 打包为单个 exe 文件 |
| `IncludeNativeLibrariesForSelfExtract=true` | 原生库嵌入 exe，运行时自动解压 |
| `-o ./publish` | 输出目录 |

**2. 获取产物**

发布完成后，在 `./publish` 目录下会生成 `TraeTools.exe`（约 150-200 MB，因为内含完整运行时）。

把这个 exe 复制到任意 Windows 10/11 电脑上双击即可运行，无需安装任何依赖。

---

### 发布为依赖框架的轻量版本

如果目标机器已安装 .NET 9 桌面运行时，可以发布更小的版本（约 10-20 MB）：

```bash
dotnet publish TraeTools.csproj -c Release -r win-x64 --self-contained false -o ./publish-lite
```

产物目录里会有 `TraeTools.exe` 和若干 dll 文件，需要目标机器预装 [.NET 9 桌面运行时](https://dotnet.microsoft.com/download/dotnet/9.0)（选择 ".NET Desktop Runtime"）。

---

### 常见问题

**Q: `dotnet restore` 很慢或失败？**

可能是网络问题。可以尝试切换到国内镜像源，在项目根目录创建 `NuGet.Config`：

```xml
<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
```

或者使用清华镜像：

```xml
<add key="tuna" value="https://mirrors.tuna.tsinghua.edu.cn/nuget/v3/index.json" />
```

**Q: 启动后提示"未检测到 WebView2 运行库"？**

签到功能的自动登录需要 WebView2。到 [微软官网](https://developer.microsoft.com/en-us/microsoft-edge/webview2/) 下载 Evergreen Bootstrapper 安装即可。也可以用手动粘贴 Token 的方式替代。

**Q: 发布后的 exe 体积太大？**

单文件模式会把整个运行时压缩进去。如果目标机器能联网，可以改用 `--self-contained false` 发布轻量版，或者用 `PublishTrimmed=true` 裁剪未使用的 BCL 程序集（注意 Avalonia 可能有反射依赖，需测试功能是否正常）。

**Q: 编译报错 "The current .NET SDK does not support targeting .NET 9.0"？**

说明安装的 SDK 版本太低。运行 `dotnet --list-sdks` 确认版本，需要 9.0.x 或更高。
